int sum(int a, int b, int c) {
  return a + b + c;
}

void main() {
  int a = 4 * 16;
  int b = sum(a, a, 10);
  if (b >= a) {
    printf("%d is greater than %d", b, a);
  } else {
    printf("%d is less than %d", b, a);
  }
}